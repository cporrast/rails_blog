<div id="<%= dom_id comment %>" class="comment">
  <div>
    <p>
      <%= comment.body %>
    </p>
    <span><%= comment.name %> - <%= comment.created_at.strftime("%B %d %Y") %></span>
  </div>
</div>
